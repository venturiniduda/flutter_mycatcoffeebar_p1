name: flutter_mycatcoffeebar_p1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  device_preview: ^1.2.0
  google_fonts: ^6.2.1
  get_it: ^8.0.0
  firebase_core: ^3.8.0
  cloud_firestore: ^5.5.0
  firebase_auth: ^5.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  email_validator: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/
    - lib/images/cardapio/